You are working on Perform AI backend + web app (mealplanai.replit.app). I want a hard purge of TWO feature families across backend + database + API + web frontend:

A) Grocery pricing feature purge (generation + storage + display)
B) Regen counter / budget logic purge (unlimited regenerations, remove counters and related UI/logic)

Non-negotiable: do NOT “disable” with flags; delete codepaths, DB fields/tables, API response fields, and UI sections. If any older clients expect fields, keep backward compatibility by returning null/empty only when unavoidable, but preference is to remove and update clients.

Deliverables:
1) Identify all places grocery pricing exists:
   - any AI prompt augmentation that requests grocery price, ingredient pricing, store totals, cost estimates
   - any services/modules named grocery, pricing, cost, estimate, budget
   - DB tables/columns storing grocery item prices, store breakdown, totals, cached price results
   - API endpoints returning grocery pricing
   - web UI components showing price/estimates
   Remove them completely.

2) Identify all places regen counter / budget exists:
   - any “regen” counters (meal swaps, day regens, plan regens) in DB, API, AI prompt logic, and UI
   - any enforcement or decrement behavior on regen
   Purge everything. Replace with unlimited regeneration:
   - any “regenerate” action should succeed without checking counters
   - remove “today’s budget” card and any counters display
   - remove tracking of remaining regens
   - remove server-side validation that blocks regen based on limits

3) Database:
   - Create and run a safe migration to drop tables/columns related to grocery pricing and regen counters (or Prisma migration if Prisma).
   - Update schema/types accordingly.
   - Ensure production deploy won’t fail due to migration history issues.

4) API contracts:
   - Update API responses to remove grocery pricing fields and regen budget fields.
   - Update docs in docs/ (api-reference.md etc) so they match reality.
   - Update any normalization logic that expected those fields.

5) Tests / validation checklist (must be executed and reported):
   - Generate 7-day meal plan: time to response should improve; no grocery pricing data returned.
   - Generate daily meal and daily workout: succeeds.
   - Regenerate meal/day/plan actions: unlimited; no counters shown; no server-side blocks.
   - Dashboard + calendar + daily detail load without errors.
   - No references to grocery pricing / regen budget remain in codebase (search-based proof: list ripgrep results for key terms).

Implementation rules:
- Make a short plan first listing files/modules you will change.
- Then implement.
- Then show a final summary with: changed files, migrations created, endpoints changed, and exact tests run + outcomes.
- If you find unclear/unknown behavior, decide and proceed; do not ask me questions unless a choice would risk data loss.